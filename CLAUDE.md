# Claude Code Global Instructions

Import the shared agent instructions:

```md
@~/.claude/AGENTS.md
```

## Claude Code Notes

- Claude Code reads `CLAUDE.md`, not `AGENTS.md`.
- Keep the shared instructions in `~/.claude/AGENTS.md`.
- Keep this file at `~/.claude/CLAUDE.md`.
- If a project also has its own `CLAUDE.md`, project-specific rules may add more context.
